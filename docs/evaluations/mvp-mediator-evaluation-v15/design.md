# MVPスキルの状態表と比較例を揃える設計

状態: 設計確定。Q1/Q2/Q3にユーザーが「1」と回答し、本文変更・評価上の扱い・範囲・受入条件に合意した。本文実装・受入検証は完了。結果は[検証記録](../../mvp-evaluation-runner.md#v15接続の検証結果)を参照。実LLM評価は未開始。

## 根拠と合意

[v14評価](../mvp-mediator-evaluation-v14/run-20260923-01/results.md)では、E01/E04がモデル内の表現と本実装の情報源を単一列へ統合し、`state-representation-source-columns-merged` が再発した。説明内容は存在し、役割の誤分類とは別の形式未達である。正式な状態表は別列だが、v14の比較例は統合列になっており、この不一致が誘因になった可能性がある。因果と修正効果は未確認。

- Q1: ユーザー回答「1」。別列を必須として維持し、比較例を正式な状態表へ揃える。
- Q2: ユーザー回答「1」。状態表と比較例の整合、および次版の配布・評価契約に限定する。検証範囲の過大報告とBの表省略への対策は別テーマに残す。
- Q3: ユーザー回答「1」。以下の具体案と受入条件で設計全体を確定。

既存の用語と責務分担を維持する。CONTEXT.mdへの語彙追加は不要で、可逆的な記入例の修正のため新規ADRは作らない。

## 本文の変更

対象は `local-skills/mvp-mediator-architecture/SKILL.md` の「状態は既存機構から導出する」。正式な状態表は維持し、直下の3列の比較例を同じ5列へ置き換える。

| 状態 | 判断に使う目的・所有者 | 抽象モデルでの区分 | 抽象モデルでの表現 | 本実装での情報源 |
| --- | --- | --- | --- | --- |
| `nextId` | コマンドと完了通知を照合するIDの発行。既存 binding／実行層 | 既存 binding／実行層の模擬 | モデル内の連番 | 既存のID発行機構。未確認なら「既存の実行境界が返す操作IDを確認する」と記す |
| `acceptedId` | 表示へ採用する操作の選択。追加の採用判断の所有者（既存 binding に必要な採用保証がない場合） | 追加の裁定 | モデル内で保持する採用ID | 採用判断の所有者が既存機構のIDを保持する |
| `tooltipOpen` | 当該View内の開閉。View | View の局所状態 | モデル内の真偽値 | 当該Viewの局所状態 |
| 期待値のsnapshot・検査回数 | assertion・検査報告だけに使う。検査側 | 検証専用の補助 | 検査側のローカル値 | 本実装には不要 |

分類の3手順は維持し、手順3の「別々に記す」を「表の別々の欄に記す」として、既存の別欄要件を例の列構成へ結び付ける。4分類の意味、全保持フィールドの説明、情報源が未確認の場合の確認対象、例の状態を追加する義務がないこと、既存機構の再利用を維持する。

成果物の状態説明は必須、事前整理は推奨。実装では抽象モデル専用の2欄を省略できる扱いも維持する。提案用の新しい例、注意事項の追加リスト、分類器、表パーサー、新しい実行モデルは追加しない。

## 評価契約と比較

v15はv14とその継承契約を引き継ぐ。C3のモデル表現と本実装情報源を別欄で説明する要件を維持し、統合列を許容する要件緩和は行わない。列分離の形式遵守、情報源の説明内容、役割分類の正確さを別々に記録する。形式未達だけで6項目の内容点を下げず、C3と既存の `state-representation-source-columns-merged` へ対応付ける。

検証範囲の過大報告とBの表省略は修正対象外だが、既存基準による判定と再発停止の対象には残す。旧run・入力・成果物・audit・採点・台帳は保持し、再採点しない。例を揃えた効果は次回の実LLM評価で確認し、静的整合やfixture成功だけでは実証しない。

E課題・固定52件checkerはv10を維持する。B/S/L課題、共通配布テンプレート、6基準/critical、C1〜C4、model/effort、隔離runtime、直列親監査、再発停止・実行上限・L投入条件、canonical metadata規則を維持する。通常経路、純粋性手順、表示導出、v13の正確な根拠表統合行の許容、v14の役割分類も維持する。

公開CLIの版登録・source hash・対応fixtureと案内は、新本文とv15契約の固定配布に必要な範囲で更新する。runtime・採点アルゴリズム・checkerは変更しない。旧runのstatus参照と新版での書込み再開拒否を維持する。

## 受入条件

1. 正式な状態表と比較例の見出し・列順が一致し、モデル表現と本実装情報源を別欄に記す。4例で目的・所有者・分類・表現・情報源の対応が明確である。
2. v14の分類手順・4分類の意味・全保持フィールドの説明・未確認の扱い・例の非強制・既存機構再利用を維持し、手順3と例に形式の不一致がない。
3. v15契約が要件緩和ではないことと形式／内容の判定を明示する。過大報告・Bの表省略への対策を混ぜず、旧記録を保持する。
4. 合意済みの公開CLI fixtureでv15本文/hashの配布、v10 E/checker維持、旧run境界を確認する。スキル形式・型・変更に適した既存テスト・Standards/Specレビューを行う。列名一致だけで意味の正確さを検証したとしない。
5. 実LLM再評価は実装完了後の明示的なempirical-prompt-tuning依頼で行う。

## 次の段階

本文変更・版登録・受入検証を同じtask worktreeで完了した。次の実LLM再評価は、別途明示されたempirical-prompt-tuningで行う。
