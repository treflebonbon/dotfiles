# MVP Mediator 評価契約 v15

状態: 実装用契約。実LLM評価は実装完了後の明示的なempirical-prompt-tuning依頼で開始する。

[v14契約](../mvp-mediator-evaluation-v14/protocol.md)とその継承契約を引き継ぐ。[確定設計](design.md)のQ1〜Q3に従い、状態表の正式な記入形式と比較例を整合させる。

## 対象と変更テーマ

対象 `local-skills/mvp-mediator-architecture/SKILL.md` のSHA-256は `4bf362316e1927b69ad2a91f70c87885b98901e25e692e3da269b688411ad679`。条件付き参照は `110b25b8ba320ac61ceb38c19142f5028aabfcc28a27cfd6b7a51b0eef288cca` を維持する。

比較例を正式な状態表と同じ5列（状態／判断に使う目的・所有者／抽象モデルでの区分／抽象モデルでの表現／本実装での情報源）へ揃える。手順3も「表の別々の欄に記す」と明記する。v14の役割による4分類、全保持フィールドの説明、未確認の情報源と確認対象、例の非強制、既存機構の再利用を維持する。成果物の説明は必須、事前整理は推奨とし、実装では抽象モデル専用の2欄を省略できる扱いも維持する。

## 判定と比較の制限

C3ではモデル表現と本実装情報源の別欄要件を維持し、統合列を許容する要件緩和は行わない。列分離の形式遵守、情報源の説明内容、役割分類の正確さを別々に記録する。内容を区別できても単一列へ統合した場合はC3 failと `state-representation-source-columns-merged` に対応付ける。形式未達だけで6項目の内容点を下げたり、情報源欠落・役割誤分類と扱ったりしない。

検証範囲の過大報告とBの表省略への対策は今回の修正対象外だが、既存基準による判定・再発停止の対象として維持する。旧run・入力・成果物・audit・採点・台帳は保持し、再採点しない。比較例の修正効果は次回の実LLM評価で確認し、静的整合やfixture成功を行動改善の実証としない。

## 維持する条件

E課題・固定52件checkerはv10、B/S/L課題、共通配布テンプレート、6基準/critical、C1〜C4、model/effort、隔離runtime、直列親監査、再発停止・実行上限・L投入条件、canonical metadata規則を維持する。通常経路、純粋性手順、表示導出、v13の正確な根拠表統合行の許容、v14の役割分類も維持する。新しい検査網羅要件は追加しない。runtime・採点アルゴリズム・checkerを変更せず、旧runのstatus参照と新版での書込み再開拒否を維持する。

## 実装検証

正式な状態表と比較例の見出し・列順、各例の目的・所有者・区分・表現・情報源、および手順3との一致をレビューする。列名一致だけで意味の正確さを検証したとしない。公開CLIの既存fixtureでv15本文/hashの配布、v10 E/checker維持、旧run境界を確認する。既存の本文コード例検査、スキル形式・型・変更に適した既存テスト、Standards/Specレビューを行う。提案用の新しい例・注意事項リスト・分類器・表パーサー・実行モデルは追加しない。
