# MVP Mediator 評価契約 v13

状態: 実装用契約。実LLM評価は実装完了後の明示的なempirical-prompt-tuning依頼で開始する。

[v12契約](../mvp-mediator-evaluation-v12/protocol.md)とv2〜v11を継承し、以下の報告要件の変更を優先する。[確定設計](design.md)のQ1〜Q3を根拠とする。

## 対象と変更テーマ

対象 `local-skills/mvp-mediator-architecture/SKILL.md` のSHA-256を `4b1ddd042affadcdacf9aa3541b33fa9c97fc4f4168153c10f5ffbabbbf18ef7` に更新する。条件付き参照は `110b25b8ba320ac61ceb38c19142f5028aabfcc28a27cfd6b7a51b0eef288cca` のままとする。

観測時点ごとの行分割を必須から推奨へ変更する。複数時点を一行にまとめても、各時点の対象値・比較相手・assertion・実行結果の対応が明確なら許容する。同じ時点のstate/effectsもそれぞれの比較相手を保つ。成功・失敗・未実装・未実行は対象ごとに区別し、区別できない場合は行を分ける。表の列構成と既存コード例を維持する。

## 判定と比較の制限

正確な統合行だけを理由にC2 fail、`observation-times-merged`、再発停止と判定しない。内容の対応が不明・誤りなら、既存のC2とE6/S6/L5等の一致項目を根拠別に判定する。別時点のassertionの流用、比較相手の取り違え、未検査値への成功主張は引き続き不適合である。要件上必要な検査は追加・実行し、未実装という記述だけで免除しない。

これは報告形式の要件緩和であり、違反減少やclear増加を報告精度の改善実績に数えない。検証主張の正確さとモデルの正しさを個別に確認する。旧run・入力・成果物・採点・台帳は変更せず、新条件での再採点や旧runの書込み再開をしない。旧runのstatus参照を維持する。

## 維持する条件

E課題・固定52件checkerはv10、B/S/L課題、共通配布テンプレート、6基準/critical、C1〜C4の枠組み、model/effort、隔離runtime、直列親監査、再発停止・実行上限・L投入条件、canonical metadata規則を維持する。通常経路、純粋性比較順序、表示導出、状態説明・分類、責務分担、Bの提案のみの扱いを維持する。新たな検査網羅要件は追加しない。状態分類・生成モデルの不具合・親checkerの改善は別テーマとし、今回変更しない。

## 実装検証

最終本文の見出し・説明・コード例・対応表が、正確な統合行と分割行をともに許容し、各検証主張の対応を必須とすることをレビューする。既存のNode標準assertによるコード例の実行検査を再利用し、表パーサーや報告生成器は追加しない。公開CLI fixtureで新本文/hashの配布、v10 E/checkerの維持、旧run境界を確認する。形式・型・変更に適した既存テストとStandards/Specレビューを行う。静的検査やfixture成功を実LLMの報告精度の証拠にしない。
