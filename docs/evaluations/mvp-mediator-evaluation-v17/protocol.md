# MVP Mediator 評価契約 v17

状態: 実装用契約。実LLM評価は実装完了後の明示的なempirical-prompt-tuning依頼で開始する。

[v16契約](../mvp-mediator-evaluation-v16/protocol.md)とその継承契約を引き継ぐ。[確定設計](design.md)のQ1〜Q3に従い、実装説明と検証の証拠の記入先、および比較式の抜粋を明確にする。

## 対象と変更テーマ

対象 `local-skills/mvp-mediator-architecture/SKILL.md` のSHA-256は `995b09a26838e05704cf96ab1034ffc6a21d7ec3c2299f80340a1774ac9c8afa`。条件付き参照は `110b25b8ba320ac61ceb38c19142f5028aabfcc28a27cfd6b7a51b0eef288cca` を維持する。

最終報告手順・根拠表・既存のstale/current記入例を置き換える。要件欄で実装の振る舞いを説明し、根拠表を検証報告の本体とする。報告する各検証主張には最終コードの比較式の短い抜粋を添える。helperは呼出しと比較内容が分かる最小限、期待値変数は主張する値が比較相手に含まれることを示す。全assertionの転記は不要で、正確な統合行・共通参照・表外要約を許容する。

比較式がない値は検査未実装、実行していなければ未実行として対象機能の未実装と区別する。要件上必要な検査は追加・実行する。提案のみは比較式・結果を検査未実装／未実行とし、検査モデルの新設は要求しない。

## 判定と比較の制限

比較式抜粋の必須化は形式条件の強化として記録する。関数名・行番号だけの不足はC2の形式未達として判定し、`assertion-excerpt-missing` に対応付ける。それだけで内容点を減点したり、`verification-scope-overclaim` と扱ったりしない。実際に未assert値を成功と報告した場合は従来どおり当該内容項目とC2で判定する。正確な統合行・要約・重複は違反にしない。

コード読解、抽象モデルの実行検査、実アプリ統合を区別する。未確認の明記で必要検査の追加・実行を免除しない。新しい網羅要件は加えない。旧run・入力・成果物・audit・採点を保持し、再採点しない。形式遵守率と報告の正確さを分けて比較し、静的整合・fixture成功を行動改善の実証にしない。

## 維持する条件

通常課題・API・6基準/critical・C1〜C4の枠組み、v10 E/checker、model/effort、隔離runtime、直列親監査、再発停止・上限・L投入条件・canonical metadata規則を維持する。v16の提案3列と状態なし一文、抽象モデル5列、役割分類と全保持fieldの説明、通常経路・純粋性・表示導出も維持する。状態なし境界は既存Bでは実LLM効果を測れず、未検証として区別する。

新本文とv17契約を公開CLIで固定配布する。runtime・採点アルゴリズム・checkerは変更せず、旧runのstatus参照と新版での書込み再開拒否を維持する。保持状態の説明不足や削除試行への別対策は混ぜない。

## 実装検証

本文・手順・表・例を確定設計と照合する。既存例のstate比較済み/effects未照合、helper比較、期待値変数、正確な統合行、B提案を文面レビューする。公開CLIの既存fixtureで新版の固定配布・hash・v10 E/checker・旧版境界を確認する。既存の本文コード例検査、形式/型検査、適切なテスト、Standards/Specレビューを行う。実LLM再評価は実装完了後の明示依頼で行う。
