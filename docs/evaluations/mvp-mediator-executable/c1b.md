# B: display-only negative control

## 最小変更

既存の合計フォーマッターで表示を整え、送信や他操作に影響しないヘルプツールチップだけを画面内の局所状態で追加する。既存の React/TanStack Query と Context 構成、業務・送信ロジックの所有者は維持する。

## 基準

| # | 判定 | 理由 |
| --- | --- | --- |
| 1 | ○ (1) | Query と既存プロジェクト構成を維持し、Effect/Atom/MVP 移行を要求しない。 |
| 2 | ○ (1) | 表示専用の変更を局所化し、Mediator、reducer、グローバル状態、実行可能な遷移モデルを追加しない。 |
| 3 | ○ (1) | 複数の Context consumer を維持し、単一 connector や転送専用コンポーネントを要求しない。 |
| 4 | ○ (1) | 既存 formatter を再利用し、ツールチップの開閉は局所状態に留める。 |
| 5 | ○ (1) | 業務規則と送信ロジックには触れず、既存の所有者を変えない。 |
| 6 | ○ (1) | 確認は「合計が既存形式で表示される」「ツールチップが開閉する」「開閉が送信・他操作を変えない」に限定する。アプリ試験は未実行であり、成功を主張しない。 |

合計: 6.0 / 6.0。critical は 1、2 ともに満点のため成功。

## Trace

| 項目 | 判定 | 記録 |
| --- | --- | --- |
| Understanding | OK | B は display-only の negative control であり、状態モデルやアーキテクチャ変更が不要と理解した。 |
| Planning | OK | 既存 formatter と局所 tooltip 状態だけを使う最小変更、3項目の挙動確認を定めた。 |
| Execution | OK | 本メモを作成した。アプリコード・モデル・テストは実行していない。 |
| Formatting | OK | B の6基準、判定理由、採点、未実行試験の扱いを記載した。 |

## 不明事項・原因・一般修正規則

不明事項: なし。原因: 表示専用の要求に対して裁定層や共有状態を追加すると責務が増える。一般修正規則: 他操作の許可・取消・進行を変えない表示は、既存の表示機構と局所状態に留める。

## 裁量・再試行

裁量: 実装や実行可能な状態遷移を作らず、確認項目を表示と局所開閉のみに絞った。再試行・やり直し: なし。
