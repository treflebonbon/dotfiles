# v2e1 — exclusive device

成果物は `v2e1.mjs` の純粋な親 Mediator 判断モデルである。`transition` は実行境界へ `acquire`、`stop`、`release` コマンドを返すだけで、I/O、scheduler、Effect runtime は持たない。子の recording / calibration フローは開始要求を親へ渡し、profile は独立の局所フローなのでモデル状態も effect も変えない。実行層は Effect が所有し、型付きエラー、defect、中断、寿命を扱う。

読取対象: `/home/ubuntu/.codex/worktrees/90ae/dotfiles/local-skills/mvp-mediator-architecture/SKILL.md`  
SHA-256: `cfcb143069299f248bdcad3dea512a5f1ee64319289a8e62fb6afa2fdea214d0`

| 状態 | 所有者 | 最新意図 | pending 実行 | 実行効果 |
| --- | --- | --- | --- | --- |
| idle | なし | なし | なし | なし |
| acquiring | なし | target | acquire target | acquire |
| active | target | target | なし | なし |
| stopping | 旧 target | target | stop 旧 target | stop |
| releasing | 旧 target | target | release 旧 target | release |
| blocked | 旧 target | target | なし | なし（retry 時のみ失敗段階を再実行） |

| 内部値 | 分類 | 用途 |
| --- | --- | --- |
| phase / owner / desired / operationId / failedStage | 本実装で追加する裁定状態 | 排他、最新意図、失敗段階を親が裁定する。 |
| stageTarget / emitted effects | 実行層の模擬 | 実行中コマンドの対象と、既存 Effect 境界への出力。I/O はしない。 |
| nextId / `selfCheck` assertions | 検証専用 | run 内の ID 一意性と遷移結果を照合する。 |

| 検査 | 期待結果 | 実測結果 |
| --- | --- | --- |
| 初期から中断なしの取得完了 | recording が active、pending は null | 実行済み: OK |
| acquisition 中の反復要求 | 最新意図だけ更新し、元の acquire ID のまま重複 I/O なし | 実行済み: OK |
| stale completion | 状態参照と effects が不変 | 実行済み: OK |
| stop 失敗 | blocked、明示 retry まで再実行なし | 実行済み: OK |
| retry と release 失敗 | 失敗した段階だけ新しい ID で再実行し、release 中に戻った最新 target を acquire | 実行済み: OK |
| profile | device 状態・effects が不変 | 実行済み: OK |

実行コマンド: `node docs/evaluations/mvp-mediator-executable/v2e1.mjs`  
実測出力: `v2e1 self-check: OK`  
提案のみの検査: なし。

| 固定チェックリスト | 判定 | 根拠 |
| --- | --- | --- |
| 1. 全待機段階で最新意図、一致する ID、重複 I/O なし | ○ | `start` は待機中に desired のみ更新し、stage ID を保持する。 |
| 2. stop/release 成功後だけ次 acquire、失敗は明示 retry | ○ | `stopping` → `releasing` → `acquiring` の順序と `blocked.failedStage`。 |
| 3. ID の新規性と stale 除外 | ○ | `nextId` は stage/retry ごとに増加し、completion は operationId 一致時だけ採用する。 |
| 4. 親と Effect の責務分離 | ○ | 親は純粋に commands を出力し、実行・エラー・寿命は Effect 境界へ残す。 |
| 5. 子フローと profile の独立 | ○ | `profile` は `unchanged`、対象選択以外の子局所状態は保持しない。 |
| 6. モデル、memo、実行検査の一致 | ○ | 上記 6 件を assertion 実行し、実測 OK を記録した。 |

Trace: Understanding OK / Planning OK / Execution OK / Formatting OK。

Unclear Issue: acquisition 成功時に要求が変わった場合、成功した取得を停止して解放する間にも要求が戻る。Cause: 最新の利用者意図と進行中実行を同一視すると、必要な完了通知または解放を失う。General Fix Rule: 意図は更新しても進行中の ID と段階を保持し、完了後に所有者と最新意図を比較して次段階を一度だけ発行する。

裁量: `desired` は acquisition 失敗時に null へ戻す。失敗後の再開は明示 `start` とし、仕様どおり自動 retry しない。`blocked` の `retry` は stop / release の失敗段階だけを新しい ID で繰り返す。

Retries: 1（初回自己検証で idle 開始時の desired 未設定を検出し、共通 acquire 開始処理で修正）。
