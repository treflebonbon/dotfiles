# B 実行メモ — display-only negative control

## Deliverable

既存の合計表示は、プロジェクトにある数値整形器で整形する。補助説明は、その表示コンポーネント内だけで開閉するツールチップにする。送信、取得、業務判定、共有 Context の構成には触れない。

## 凍結基準

| # | 判定 | 理由 |
| --- | --- | --- |
| 1 | ○ | 既存の React/TanStack Query とプロジェクト規約を維持する。Effect、Atom、MVP 移行を追加しない。 |
| 2 | ○ | 合計の表示整形と局所ツールチップだけなので、Mediator、reducer、グローバル状態、実行可能な遷移モデルは不要である。 |
| 3 | ○ | compound components が共有 Context を複数箇所で読む構成をそのままにし、単一 connector や転送コンポーネントを設けない。 |
| 4 | partial | 既存の整形器を使い、ツールチップの開閉状態は表示コンポーネントに局所化する方針は確定できる。整形器・既存ツールチップの実名と API は入力にないため、実装時に対象コンポーネントで確認する。 |
| 5 | ○ | 送信と業務規則の所有者を変更しない。ツールチップの開閉は他操作の許可、取消、進行に影響しない。 |
| 6 | ○ | 確認は、合計が既存と同じ規則で整形されること、補助説明を開閉しても合計以外の表示・送信状態が変わらないことに限る。未実行のアプリテストは成功と記載しない。 |

## Trace

| フェーズ | 状態 | 記録 |
| --- | --- | --- |
| 1. 変更範囲の判定 | OK | 表示専用で、操作フローを変えない。 |
| 2. 責務の配置 | OK | 表示整形とツールチップの局所状態は Passive View に置ける。 |
| 3. 既存 API への接続 | skipped | この課題は最小変更・確認メモだけを求め、実装対象の formatter/tooltip API は提示されていない。 |
| 4. 振る舞い確認の設計 | OK | 整形結果と、ツールチップが局所表示だけを変えることを確認対象にした。 |

## Unclear

| Issue | Cause | General Fix Rule |
| --- | --- | --- |
| 使用中の formatter とツールチップの API が特定できない。 | シナリオには既存利用の要件だけがあり、対象コンポーネントの実装情報がない。 | 実装が必要になった時点で対象コンポーネントと既存 formatter を確認し、既存 API をそのまま呼ぶ。表示専用の変更に新しい裁定層を足さない。 |

## 任意選択

- ツールチップの開閉操作と文言は、既存コンポーネントのアクセシビリティ規約に従う。
- 既存ツールチップがなければ、表示コンポーネント内の最小の局所状態で扱う。

## 再試行

0 回。アプリケーションコードとテストは実行していない。
