# MVP Mediator 評価契約 v5

状態: 実装用契約。実LLM評価は次の明示的なempirical-prompt-tuning依頼で開始する。

[v4契約](../mvp-mediator-evaluation-v4/protocol.md)（SHA-256 `f54c45743f598ca025b5d154bbe5310d53687ad476559216391ca328bd85184a`）と、その基礎となるv2/v3契約を継承する。差分は対象本文の版だけである。

## 対象と仮説

対象 `local-skills/mvp-mediator-architecture/SKILL.md` のSHA-256を `fe23163138cf384063853b8ae05dd710195e1fd6345e60f565d66347246905a5` に更新する。条件付き参照は変更しない。

変更テーマは最終成果物との照合による完了手順。既存の検証メモ指示を、最終編集後にケース目的と操作列、保持状態と用途・区分・取得元、検証主張とassertion・実行結果を対応付ける順序へ組み替える。検査追加後の通常経路への反復混入、状態説明の欠落・誤分類、検証範囲の過大報告を減らせるかを検証する。既存のケース分離・状態説明の定義を参照し、同じ規則を重複追加しない。

v4は独立したEの2実行で通常経路への反復混入が再発して停止した。最初のEは状態説明が成立したが、Sで誤分類、次のEで説明欠落と検証範囲の過大報告も観測した。変更の効果は未検証。

## 維持する条件

E/B/S/Lの課題・API・6基準・critical指定・C1〜C4、固定checker、model/effort、隔離条件、配布テンプレート、直列監査、復帰済みエラーの扱い、再発停止・上限・L投入条件・canonical metadata規則はv4のまま維持する。Bは提案のみでモデル不要。完了手順を配布テンプレートへ重複追加せず、新本文の全文として渡す。

旧契約・runの入力・成果物・採点は保持する。新規runはv2〜v5契約と新本文のhash・実行入口を固定する。旧runのstatus参照は維持し、新版による書込み再開は認めない。

## 実装Contract

受入条件:

1. 本文に最終編集後の3つの照合と、修正した場合の検査・照合更新を明記する。提案のみは未実行として扱い、既存の責務・状態・検証要件と適用範囲を保つ。
2. 公開CLIのinit/prepareで新本文とv2〜v5契約を固定・配布する。課題・配布テンプレート・採点とruntimeは変更しない。
3. 旧評価記録を保持し、隔離・承認・監査・再発停止・上限ゲートを維持する。

検証境界は合意済みの公開CLI fixture。専用Batsの新版固定・配布テストでred→greenを確認し、型確認、全体テスト、Standards/Specレビューを行う。本文の行動改善はfixtureの検証対象ではなく、別の明示的な実測評価に残す。

非目標: 今回の実LLM評価、採点基準の変更・自動化、汎用版選択、過去runの修正・再採点、配備。
