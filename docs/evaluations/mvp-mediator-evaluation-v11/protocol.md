# MVP Mediator 評価契約 v11

状態: 実装用契約。実LLM評価は実装完了後の明示的なempirical-prompt-tuning依頼で開始する。

[v10契約](../mvp-mediator-evaluation-v10/protocol.md)とv2〜v9を継承し、以下の本文変更と適用上の差分を優先する。[確定設計](design.md)のQ1〜Q3を根拠とする。

## 対象と変更テーマ

対象 `local-skills/mvp-mediator-architecture/SKILL.md` のSHA-256を `32126db46011ee957fe8cc10ec63d8a93c856a3811ea3bbf1be662bb7d8bad1a` に更新する。条件付き参照は `110b25b8ba320ac61ceb38c19142f5028aabfcc28a27cfd6b7a51b0eef288cca` のままとする。

純粋な遷移の自己検査に、初回直後・再呼出し直後の入力比較、再呼出し前の初回結果snapshot、初回結果の保持・結果同値の比較を示す短い具体例を置く。snapshot/equalityは既存の状態表現に合わせ、比較不能なら未確認と必要な確認方法を記録する。E専用の通常データ制約を一般アプリへ拡大しない。状態分類・根拠表の指示は変更しない。

## 維持する条件と比較の制限

E課題の配布元と親checkerはv10のまま。B/S/L課題・6基準・critical指定・C1〜C4の枠組み、共通配布テンプレート、runtime隔離、model/effort、直列監査、E/B/S各3組、再発停止・上限・L条件・canonical metadata規則を維持する。実行者には新本文を全文渡す。

Eでは既に課題が要求していた比較順序を本文の例で支える。S/Lでは共通手順が新本文で明示されるため、C2の判定に適用し、その変化を単なる指示改善と要件の明示化に分けて記録する。旧S/Lの成功を遡及的に失敗へ変えない。表現に適した比較を使っているか、各呼出し後の比較があるか、初回結果snapshotがあるかをコード・実出力と照合する。機能・報告精度・状態分類と分けて観察し、clear数の単純比較で改善を主張しない。

有限検査の成功を全実行の純粋性の証明とせず、親checkerの結果を実行者自己検査の実績へ加えない。Bは提案のみ。旧契約・checker・run・入力・成果物・採点を保存し、旧runのstatus参照だけを維持する。新版での書込み再開・再採点は認めない。

## 実装検証

本文の具体例を直接実行し、通常データの純粋モデルが成功すること、初回入力変更を再呼出しで戻す不正例を初回直後に検出すること、再呼出し時の入力変更と返却object再利用による書換えを検出することを確認する。公開CLI fixtureで新版本文・契約の固定配布、v10 E/checkerの維持と旧版境界を検査する。型・形式検査、既存テスト、Standards/Specレビューを行う。これらを実LLMの行動改善の証拠にはしない。
