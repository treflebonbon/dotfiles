# 提案のみの場合の状態説明を明確にする設計

状態: 設計確定。Q1/Q2/Q3にユーザーが「1」と回答し、具体案・範囲・受入条件を含む設計全体に合意した。本文・配布契約・CLIの実装と受入検証は完了。結果は[検証記録](../../mvp-evaluation-runner.md#v16接続の検証結果)を参照。実LLM再評価は未開始。

## 根拠と対象

[v15評価](../mvp-mediator-evaluation-v15/run-20260923-01/results.md)では、E01/S03/E04が状態説明を5列で記載した一方、B02/B05は提案するtooltip局所状態の説明表を省略した。B02は「実行モデル、抽象状態表…は表示のみの課題では不要」と明記した。実行モデル不要の指定を、提案する保持状態の説明不要へ広げたことが確認できる。B05の原因は推定に留める。

対象はlocal-skills/mvp-mediator-architecture/SKILL.md。既存の適用手順4は提案にも保持状態の説明を要求するが、状態表の抽象モデル専用2欄を省略できる対象は「実装」と記す。説明対象の状態がない場合の記載形式は明記されていない。

今回は提案の状態説明を完了条件へ結び付けるテーマ。v15の抽象モデル5列例・役割分類・純粋性・正確な根拠表要約を維持する。検証範囲の過大報告は別テーマとして残す。旧評価・採点を変更しない。

## 合意した判断

- Q1: ユーザー回答「1」。抽象モデルを作らない提案は、状態／目的・所有者／本実装の情報源の3列で説明する。抽象モデルは従来の5列を維持する。
- Q2: ユーザー回答「1」。説明対象の保持状態がない場合は「説明対象の保持状態なし」と一文で明記し、空表を作らない。
- Q3: ユーザー回答「1」。以下の具体案・範囲・評価上の扱い・受入条件で設計全体を確定。

formatter再利用だけの提案を境界例にする。tooltipの開閉など、提案で既存状態を使う場合は説明対象がある。画面全体の無関係な状態を新たに棚卸しする指定にはしない。「提案」は実装コードを作らないという意味で、成果物として抽象実行モデルを作る場合は5列の側へ分類する。

## 具体的な本文案

変更対象は「適用範囲と進め方」の手順4、「成果物と完了条件」の冒頭、状態表直下の省略規則とする。状態説明の詳細は既存の表を正本とし、完了条件の近くには提案の到達点を示す。新しい例ファイルや説明表のパーサーは追加しない。

### 手順4

末尾の「提案のみの場合も、提案で保持する状態を説明する」を次に置き換える。他の手順と事前整理の推奨は維持する。

> 提案だけの場合の形式と、説明対象がない場合の扱いは次の「成果物と完了条件」に従う。

### 成果物と完了条件の冒頭

現行の冒頭段落を次へ置き換える。

> 抽象モデルを作らない設計提案では、操作方針、提案で追加・保持する状態の説明表、具体的な確認ケースと期待値、未実行の範囲を示す。既存の局所状態を使う提案も説明対象とする。状態表は「状態／判断に使う目的・所有者／本実装での情報源」の3列を使う。局所状態だけを扱う提案なら、その状態の行で説明を完了できる。説明対象の保持状態がなければ「説明対象の保持状態なし」と一文で記し、空表は作らない。
>
> 実装では既存プロジェクトの検証方法を使う。表示変更のために抽象モデルや状態機械を作らない。

局所状態の例としてtooltipは既存の比較例にあるため、本文へ重複する新しい例は追加しない。3列名は正式な5列の対応する列名と揃える。

### 状態表直下の省略規則

> 抽象モデルを作らない提案・実装では、抽象モデル専用の2欄を省略できる。提案の完了条件は前述の「成果物と完了条件」に従う。抽象モデルの状態説明は次の手順で揃える。

続く3手順・正式5列・比較例4行・4分類・全保持フィールドの説明・情報源未確認時の確認対象・例の非強制は維持する。提案の情報源も既存機構、追加する所有者、または未確認＋具体的な確認対象で説明する。未確認を接続検証済みと扱わない。

## 評価上の扱いと範囲

v16契約は提案の3列を明示的に許容する。既存の省略対象が「実装」だけだったため、提案への適用範囲の明示・拡張として記録し、完全に同一の形式条件だったとは主張しない。保持状態がある提案の表自体は引き続き必須で、文章だけの省略はC3のstate-table-missingとなる。B02/B05の旧採点は変更しない。状態なしの一文による完了は、説明対象がない場合だけの規則であり、tooltipなどの状態がある提案を免除しない。

通常課題・API・6基準/critical・C1〜C4の枠組み、v10 E/checker、model/effort、隔離runtime、直列監査、再発停止・上限・L投入条件、canonical metadataの規則を維持する。抽象モデル5列の分離と分類、根拠表の正確な統合要約、純粋性・通常経路・表示導出も維持する。検証範囲の過大報告への対策は混ぜない。

実装時は新本文とv16契約の固定配布に必要な公開CLIの版登録・source hash・既存fixture・案内を更新する。runtime・採点アルゴリズム・checkerは変更しない。旧runのstatus参照と新版での書込み再開拒否を維持する。実LLM再評価は実装完了後の明示的なempirical-prompt-tuning依頼で行う。

## 受入条件

1. 抽象モデルを作らない提案は、保持状態を3列で説明する完了条件が明確で、表示変更の完了条件の近くにある。既存状態を使う場合も対象に含む。
2. 説明対象の状態がない提案は一文で明記し空表不要。formatterだけの提案と、tooltip状態を使う提案を文面レビューで区別できる。
3. 抽象モデルを成果物に含む場合は従来の5列を使い、役割分類・モデル表現・本実装の情報源を維持する。提案という呼称で専用2欄を省略しない。
4. v16契約に形式条件の明示・拡張と比較上の制限を記し、旧評価を再採点しない。本文の読みやすさやfixture成功を行動改善の実証にしない。
5. 合意済みの公開CLI fixtureで新版の配布・hash・v10 E/checker・旧版境界を確認する。形式検査・型検査・変更に適した既存テスト・Standards/Specレビューを行う。状態なし境界の実LLM効果は既存B課題では測れないため、文面レビューと区別して未検証と記す。

## 次の段階

本文変更・版登録・受入検証を同じtask worktreeで完了した。次の実LLM再評価は、別途明示されたempirical-prompt-tuning依頼で行う。今回は既存語彙の適用条件を明確にするもので、CONTEXT.mdへの新語追加は不要。可逆的なskill説明の変更のため新規ADRは作らない。

## 実装レビューでの明確化

Specレビューで受入条件1の既存局所状態の包含を本文にも明記する指摘を受け、冒頭の具体案へ「既存の局所状態を使う提案も説明対象とする」を補った。合意済み要件の明記であり、状態なし例外や評価範囲を変更しない。
