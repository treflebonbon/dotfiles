{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "test -x \"${IMPECCABLE_BIN:-}\" && test -f \"$HOME/.agents/skills/impeccable/scripts/impeccable\" || exit 0; output=\"$(IMPECCABLE_HOOK_QUIET=1 sh \"$HOME/.agents/skills/impeccable/scripts/impeccable\" hook 2>/dev/null)\" || exit 0; printf '%s' \"$output\"",
            "timeout": 5
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test -x \"${IMPECCABLE_BIN:-}\" && test -f \"$HOME/.agents/skills/impeccable/scripts/impeccable\" || exit 0; output=\"$(IMPECCABLE_HOOK_QUIET=1 sh \"$HOME/.agents/skills/impeccable/scripts/impeccable\" hook 2>/dev/null)\" || exit 0; printf '%s' \"$output\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
