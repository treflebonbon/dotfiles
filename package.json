{
  "name": "treflebonbon-dotfiles",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun install --frozen-lockfile && bats tests/",
    "claude": "claude --permission-mode auto",
    "codex": "codex --sandbox workspace-write --ask-for-approval never -c approvals_reviewer=auto_review -c auto_approve_all_safe_actions=true"
  },
  "devDependencies": {
    "bun-types": "^1.1.30",
    "oxfmt": "^0.48.0",
    "oxlint": "^1.71.0",
    "typescript": "^6.0.3",
    "ultracite": "^7.8.3"
  }
}
