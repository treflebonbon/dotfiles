You are modeling an existing implementation for a Japanese ROP flow report. All permitted source and optional machine-extracted evidence are supplied below. Do not use tools, browse, execute the target, read other files, or access other runs. Output exactly one JSON object conforming to the report format below, without a Markdown fence or explanation outside JSON. This is static source analysis, not a record of execution or an exhaustive proof. Do not claim that a type union makes every error outcome feasible on every branch.

Use roughly 4–7 business stages where possible, but preserve separate handler scopes and necessary branches. Short Japanese labels belong in the diagram; types, operator names and supporting explanations belong in summary/notes. Pure plumbing may be grouped. Include ordinary success, early failure and recovery outcomes when supported. Each named path must be feasible from the supplied source conditions.

Bind continues only on success. Errors skip subsequent success-only operations. Error mapping stays on the error channel. Recovery handles only matching errors in its scope; its own failure and unmatched errors must propagate. Early returns never continue later in the same function. A caller may handle returned errors separately. Rust ? may act on Option or Result and is scoped to the enclosing function, closure or async block. Method spelling alone does not establish standard Result behavior. Effect requirements R are not the error channel E. Fallible tap can fail. Ordinary typed-error handlers do not catch every defect or interruption. Client/effect construction is different from execution of the effect. Retries are bounded by the provided policy; do not invent counts or delays. Parallel work must not be serialized in the diagram. Preserve named boundaries for unavailable implementation details.

Supplemental evidence may be incomplete. Syntax containment is not name or type resolution. Use the source to check evidence and mark concrete unknowns rather than guessing missing semantics. Cite only supplied source file names and original line numbers. Every real operation/boundary has a supporting source range. Synthetic bypass/terminal nodes may omit source. Explain omitted behavior in limitations.
